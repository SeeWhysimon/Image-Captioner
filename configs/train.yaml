# train.yaml

num_epochs: 10
print_every: 1
save_every: 5
device: cuda  

optimizer:
  type: Adam
  lr: 0.001
  weight_decay: 0.0001

scheduler:
  type: StepLR
  step_size: 50
  gamma: 0.5

criterion: 
  type: CrossEntropyLoss  
  ignore_idx: 0 

save_dir: ./logs

checkpoint_path: null
history_path: null

misc:
  clip_grad_norm: 5
